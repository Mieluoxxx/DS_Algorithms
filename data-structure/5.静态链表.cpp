#include <iostream>
using namespace std;

#define MAXSIZE 10
#define ElemType int

typedef struct Node {
    ElemType data;
    int next;   // 指向下一个元素的下标
}SLinkList[MAXSIZE];

int main() { 
    return 0; 
}